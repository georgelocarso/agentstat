{
  "name": "@agentstat/shared",
  "version": "0.1.0",
  "description": "Shared types and sanitization utilities for agentstat",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "type": "module",
  "scripts": {
    "build": "tsc",
    "test": "vitest run"
  },
  "devDependencies": {
    "typescript": "^5.8.0",
    "vitest": "^3.0.0"
  }
}
